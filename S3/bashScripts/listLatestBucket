#type in console
#./S3/bashScripts/listLatestBucket

echo "Listing the newest bucket :-)"

aws s3api list-buckets --output json | jq -r '.Buckets | sort_by(.CreationDate) | reverse | .[0] | .Name'

echo "..."
